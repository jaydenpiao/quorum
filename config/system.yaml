app:
  name: quorum
  environment: local
  log_path: data/events.jsonl

server:
  host: 0.0.0.0
  port: 8080

ui:
  title: Quorum Operator Console

http:
  cors_allowed_origins:
    - http://127.0.0.1:8080
    - http://localhost:8080
  rate_limit_default: "120/minute"
  rate_limit_demo: "5/minute"
